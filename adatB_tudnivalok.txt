Zh elmélet + gyakorlat

Honlapon ha be kell jelentkezni:
user: hallgato
pw: eltegyak

http://medusa.inf.elte.hu/oradoc11/index.htm

2.gyak

röpiben null, unknown

- Milyen azonosítójú osztályon dolgoznak a dolgozók?
SELECT OAZON FROM DOLGOZO;

- Adjuk meg a dolgozók között elõforduló foglalkozások neveit!
SELECT DISTINCT OAZON FROM DOLGOZO;

- Kik azok a dolgozók, akiknek a jutaléka ismert?

select OAZON, DNEV, FIZETES, fizetes + NVL(JUTALEK, 0) "Fizetés"
from dolgozo
where JUTALEK is not null;

- Adjuk meg a dolgozók nevét, az éves fizetését és hány hete állt munkába!

3.gyak

Relációs algebra szintaktikája:

PI_oszlopok(G_feltétel(Táblanév))

-Konkatenáció

select DNEV || ' foglalkozasa ' || FOGLALKOZAS részletek
from DOLGOZO;

-Karakter és integer sorozat konkatenálása

select DNEV || ' foglalkozasa ' || FOGLALKOZAS || ' és fizetése ' || TO_CHAR(fizetes)
részletek

-Agregátor függvények
select min(FIZETES), max(FIZETES), sum(FIZETES), avg(FIZETES) from dolgozo group by FOGLALKOZAS;

select FOGLALKOZAS, count(*), count(oazon), min(FIZETES), max(FIZETES), sum(FIZETES), round(avg(FIZETES),2) from dolgozo group by FOGLALKOZAS;

Ajánlott házi:
3.gyak anyagának befejezése


from DOLGOZO;

